import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'app.ionic.angular.action-labs',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
